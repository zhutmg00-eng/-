# 系统架构文档

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│              Streamlit 前端 (用户交互层)                  │
│  车队数据输入 │ 碳资产仪表盘 │ 政策问答对话              │
└────────┬────────────┬──────────────┬────────────────────┘
         │            │              │
         ▼            ▼              ▼
┌─────────────────────────────────────────────────────────┐
│           FastAPI 后端 (API + 业务逻辑层)                │
│                                                         │
│  ┌──────────────────┐  ┌──────────────────────────┐    │
│  │ 碳排放计算引擎    │  │ RAG 政策解析引擎          │    │
│  │ • 排放因子换算    │  │ • 知识库(ChromaDB)       │    │
│  │ • 配额缺口估算    │  │ • 语义检索(向量匹配)     │    │
│  │ • 碳价参考预测    │  │ • LLM调用(Prompt引擎)    │    │
│  └──────────────────┘  └──────────────────────────┘    │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │          共享数据层 (企业碳画像)                    │  │
│  └──────────────────────────────────────────────────┘  │
└────────┬────────────┬──────────────┬────────────────────┘
         │            │              │
         ▼            ▼              ▼
┌─────────────────────────────────────────────────────────┐
│                    数据存储层                            │
│  ChromaDB │ JSON/CSV │ 政策文档(Markdown缓存)           │
└─────────────────────────────────────────────────────────┘
```

## 核心设计原则

1. **计算与推理分离** — 碳排放量用规则引擎（确定性），政策解读用LLM（概率性）
2. **渐进式开发** — 每个阶段产出可独立运行、可演示
3. **MVP优先** — 先做"输入车队→输出碳配额缺口和成本"闭环
4. **模拟数据先行** — 前6个月用公开数据和模拟参数

## 模块说明

### 碳排放计算引擎 (`src/engine/`)
- `emission_factors.py` — 排放因子数据库（查表）
- `calculator.py` — 碳排放基线计算（数学公式）
- `quota.py` — 配额缺口估算
- `carbon_price.py` — 碳价参考与成本预测

### RAG政策助手 (`src/rag/`)
- `crawler.py` — 政策文档爬取
- `parser.py` — 文档解析(PDF/DOCX/HTML) + 清洗 + 切分
- `vector_store.py` — ChromaDB向量知识库管理
- `generator.py` — Prompt模板 + LLM调用

### API层 (`src/api/`)
- `main.py` — FastAPI应用入口
- `POST /api/calculate` — 碳排放计算
- `POST /api/ask` — 政策问答
- `GET /api/vehicle-types` — 车型列表
- `GET /api/kb/stats` — 知识库统计
- `POST /api/kb/ingest` — 导入政策文档

### 前端 (`src/ui/`)
- `app.py` — Streamlit主入口
- 页面：首页、碳资产盘点、政策顾问、排放因子表、生成报告
