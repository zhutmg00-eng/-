# ============================================================
# 碳资产助手 — 环境变量模板
#
# 使用方法：
#   cp .env.example .env
#   编辑 .env 填入实际值
# ============================================================

# ----------------------------------------------------------
# API 安全
# ----------------------------------------------------------
# API Key（FastAPI 鉴权用，生产环境必须设置）
APP_API_KEY=your-api-key-here

# CORS 允许的来源（逗号分隔）
CORS_ORIGINS=http://localhost:8501,http://localhost:3000

# ----------------------------------------------------------
# LLM API（可选，用于政策问答功能）
# ----------------------------------------------------------
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=
ZHIPU_API_KEY=
OPENAI_API_KEY=

# LLM 模型选择
LLM_MODEL=deepseek-chat

# Embedding 模型（openai / bge-local）
EMBEDDING_MODEL=openai

# ----------------------------------------------------------
# Docker 部署
# ----------------------------------------------------------
# API 服务地址（Streamlit 调用 FastAPI 时使用）
API_BASE_URL=http://localhost:8000

# ----------------------------------------------------------
# 日志级别
# ----------------------------------------------------------
LOG_LEVEL=info
